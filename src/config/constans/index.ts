export const isPhone = document.body.clientWidth < 480;

export enum PageEnum {
  ANALYTIC = "analytic",
  PROFILE = "profile",
  MESSAGE = "message",
  POSTS = "posts",
  STATISTIC = "statistic",
  USERS = "users",
}
